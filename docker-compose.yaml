version: '3'
services:

  postgres:
    image: postgres:18
    ports:
      - '5552:5432'
    environment:
      POSTGRES_DB: db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    healthcheck:
      test: ["CMD-SHELL", "pg_isready", "-U", "user"]
